import React from 'react';
import Menu from './Menu'

const Header = () => {

    return (
        <header>
            <div>LOGO</div>
            <Menu />
        </header>
    );
}

export default Header;